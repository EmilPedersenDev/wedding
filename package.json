{
  "name": "wedding",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "functions:serve": "supabase functions serve --env-file supabase/.env.local --no-verify-jwt",
    "functions:serve:debug": "supabase functions serve --env-file supabase/.env.local --no-verify-jwt --inspect-mode brk"
  },
  "dependencies": {
    "nuxt": "^4.2.2",
    "vue": "^3.5.25",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "sass": "^1.97.3"
  }
}
